/**
 * sessionStorage key for unsaved contract template preview (AURA-133).
 * Must open preview same-tab — installed PWA `_blank` does not share this store (AURA-444).
 */
export const CONTRACT_PREVIEW_STORAGE_KEY = "aura-contract-preview";
